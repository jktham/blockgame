#include <GLFW/glfw3.h>
#include <glm/glm.hpp>

#include <vector>

#include "global.h"
#include "light.h"

void Light::update()
{
	//direction = glm::vec3(50.0f, 50.0f + sin(current_frame) * 100.0f, cos(current_frame) * 50.0f);
}